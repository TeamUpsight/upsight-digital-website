---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ServicePage from '../../components/seo/ServicePage.astro';
import { serviceSchema } from '../../lib/seo';
import { servicePages } from '../../lib/service-pages';
const page = servicePages['ecommerce-tracking'];
---
<BaseLayout title={page.title + ' | Upsight Digital'} description={page.description} jsonLd={serviceSchema(page.title, page.schemaDescription, '/services/ecommerce-tracking/')}><ServicePage {...page} /></BaseLayout>
