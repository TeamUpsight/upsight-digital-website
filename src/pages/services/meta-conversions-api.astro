---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ServicePage from '../../components/seo/ServicePage.astro';
import { serviceSchema } from '../../lib/seo';
import { servicePages } from '../../lib/service-pages';
const page = servicePages['meta-conversions-api'];
---
<BaseLayout title={page.title + ' | Upsight Digital'} description={page.description} jsonLd={serviceSchema(page.title, page.schemaDescription, '/services/meta-conversions-api/')}><ServicePage {...page} /></BaseLayout>
